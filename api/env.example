# API Environment Variables
# Copy this file to api/.env and fill in your actual database values

# Database Configuration (SQL Server)
SQL_SERVER=your-sql-server-host
SQL_DATABASE=your-database-name  
SQL_USERNAME=your-username
SQL_PASSWORD=your-password
SQL_DRIVER=ODBC Driver 18 for SQL Server
SQL_TRUST_CERT=yes

# Flask Configuration (these have sensible defaults)
FLASK_DEBUG=False
FLASK_PORT=5001
FLASK_HOST=0.0.0.0

# Gunicorn Production Server Configuration
# See gunicorn.conf.py for all settings. These env vars can override config file.
# Log level: debug, info, warning, error, critical
GUNICORN_LOG_LEVEL=info
# Bind address (default: 0.0.0.0:5001)
# GUNICORN_BIND=0.0.0.0:5001

# Authentication Configuration
JWT_SECRET=your-random-secret-key-here-generate-with-openssl-rand-hex-32
MICROSOFT_REDIRECT_URI=http://localhost:3000/auth/callback
MICROSOFT_CLIENT_ID=your-client-id
MICROSOFT_CLIENT_SECRET=your-client-secret
MICROSOFT_TENANT_ID=your-tenant-id
ALLOWED_DOMAINS=yourdomain.com
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# UniFi Access Configuration (for Entry Logs widget)
UNIFI_ACCESS_HOST=172.19.1.1
UNIFI_ACCESS_PORT=12445
UNIFI_ACCESS_TOKEN=your-unifi-access-api-token

# AC Infinity Configuration (for Fan Controller widget)
AC_INFINITY_EMAIL=your-ac-infinity-account-email
AC_INFINITY_PASSWORD=your-ac-infinity-account-password
